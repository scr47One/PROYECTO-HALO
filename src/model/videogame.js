export class Videogame {
    constructor({id, title, plot, cover, trailer }) {
        this.id = id
        this.title = title
        this.plot = plot
        this.cover = cover
        this.trailer = trailer
    }
}